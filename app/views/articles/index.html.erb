<p id="notice"><%= notice %></p>


<div class="container">
  <h1>Hung's blog</h1>
  <h3>My first Rails application for coderschool</h3>
  <nav class="navbar navbar-light bg-faded">
    <ul class="nav navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Features</a>
      </li>
      <li class="nav-item">
         <%= link_to 'About', "articles/about", class: "nav-link" %>
      </li>
    </ul>
  <!--   <form class="form-inline pull-xs-right">
      <input class="form-control" type="text" placeholder="Search">
      <button class="btn btn-success-outline" type="submit">Search</button>
    </form> -->

	<%= form_tag(articles_path, :method => "get", id: "search-form", class: "form-inline pull-xs-right" ) do %>
	<%= text_field_tag :search, params[:search], placeholder: "Search", class: "form-control"%>
	<%= submit_tag "Search", class:"btn btn-success-outline" %>
	<% end %>
  </nav>

  <% if @articles.present? %>
  		<%= render @articles  %>
  <% else %>
  		<p>There are no article containing the term(s) <%= params[:search] %>.</p>
  <% end %>

  <%= link_to 'New Article', new_article_path, class: "btn btn-primary" %>
</div>
